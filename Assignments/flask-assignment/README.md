# Flask MongoDB Assignment

## Features

- Flask API returning JSON from server/file
- Form submission with MongoDB Atlas
- Success page redirect
- Error handling on same page

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r server/requirements.txt
python server/app.py
```
